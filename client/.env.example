# No env vars needed — Vite proxy handles /api -> http://localhost:5000
# If deploying separately, create .env with:
# VITE_API_URL=https://your-backend.com/api
# Then update src/utils/api.js baseURL to: import.meta.env.VITE_API_URL || '/api'
